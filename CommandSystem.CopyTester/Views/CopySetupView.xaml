<UserControl x:Class="CommandSystem.CopyTester.Views.CopySetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CommandSystem.CopyTester"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBox Text="{Binding Options.SourcePath}"/>
            <TextBox Text="{Binding Options.DestinationPath}"/>
            <CheckBox Content="Include hidden files" IsChecked="{Binding Options.IncludeHidden}" />
            <Button Content="Start Copy" Command="{Binding StartCopyCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
