<UserControl x:Class="CommandSystem.CopyTester.Views.CopyProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CommandSystem.CopyTester"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <ProgressBar Value="{Binding Percentage, Mode=OneWay}" Maximum="100" Height="20" />
            <TextBlock Text="{Binding StatusText, Mode=OneWay}" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Pause" Command="{Binding PauseCommand}"/>
                <Button Content="Resume" Command="{Binding ResumeCommand}"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
